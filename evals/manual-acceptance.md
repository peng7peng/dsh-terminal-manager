# 人工验收清单

本文件是**唯一**的人工验收清单（原 `docs/test-plan.zh.md` 的脚手架指令已随测试补全完成而废弃，重复项并入本页）。照着逐条跑，每条勾上。跑完 = MVP 全功能人工验证通过。

## 前置准备

```sh
# 1. 起两台模拟设备
cd /d/myProject/dsh/dsh-terminal-manager
node scripts/mock-device.mjs 2323 &
node scripts/mock-device.mjs 2324 &

# 2. 起 DSH
cd /d/myProject/dsh/deepseek-harness
pnpm dsh --profile tm-dev --port 4580 --no-open &
# 启动后终端会打印一行 "dsh web: http://127.0.0.1:4580/?token=..."——必须用这条带 token 的完整地址打开
# （新版 DSH 有登录保护，直接开 http://127.0.0.1:4580 会显示 "authentication required"；
#   第一次带 token 打开后浏览器记住登录，之后刷新不用再带）。硬刷新（Ctrl+F5）加载最新 client.js
# 报 "EADDRINUSE 4580" = 上一个 DSH 还在跑：回到它的窗口找 token 地址，或 netstat -ano | findstr :4580 找到 PID 后 taskkill /PID <pid> /F 再重启




```

* * *

## 组一：配置管理

### ☐ S1 新建连接（保存）

-   **操作**：点侧边栏底部「🖥️ 终端管理」→ 工作区打开 → 右侧「连接」面板 → 协议选 **Telnet** → 填 名称`mock-1`、主机`127.0.0.1`、端口`2323` → 点「保存」
-   **预期**：不报错；「收藏」出现 mock-1

### ☐ S1-临时连接（不入库）

-   **操作**：不点保存，直接点「连接」按钮
-   **预期**：终端窗格出现，显示横幅 `Mock Router :2323`

### ☐ S12 编辑连接

-   **操作**：点「最近连接」里的 mock-1 卡片 → 表单回填 → 改名称为 `mock-1-renamed` → 点「保存」
-   **预期**：列表里名字变成 mock-1-renamed
-   注：编辑后想取消，点「取消」按钮（已加）

### ☐ S12 删除连接

-   **操作**：右键 mock-1 卡片 → 删除
-   **预期**：从列表消失

* * *

## 组二：连接与观察

### ☐ S2 一键连接

-   **操作**：新建/选中一条 2323 的连接 → 点「连接」
-   **预期**：状态点变绿（已连接）；终端窗格出现，显示横幅 + `router>` 提示符

### ☐ S3 多设备同屏

-   **操作**：再连一台 2324（名称 mock-2）
-   **预期**：两个终端窗格并排（或随宽度自动换列），都在线；窗格填满终端模块、无空隙

### ☐ S13 隐藏/显示窗格

-   **操作**：点「活跃会话」里某条会话条目
-   **预期**：该窗格隐藏（连接不断，条目变灰）；再点回来恢复

### ☐ S14 多终端最大化

-   N/A：当前版本无最大化按钮（待办，见 `plan.md` 后续待办）

* * *

## 组三：手动操作

### ☐ S4 手动敲命令

-   **操作**：在某个终端窗格里直接敲 `show version` 回车
-   **预期**：能看到自己敲的字（设备回显）；回车后显示 `MockOS Version 1.0.4`
-   注：退格在输入行空了之后不再删提示符（已修）

### ☐ S5 广播到全部

-   **操作**：底部广播栏（两行布局）输入 `show version` → 目标选「全部」→ 点「发送」
-   **预期**：两个终端同时回显命令 + 版本输出

### ☐ S6 广播到部分

-   **操作**：取消选一个芯片（只留一个目标）→ 发送
-   **预期**：只有选中的那台回显

* * *

## 组四：AI 自动化（核心价值）

### ☐ S7 AI 单机测试

-   **操作**：在聊天框发：`连上 127.0.0.1:2323 跑 show version 告诉我结果`
-   **预期**：AI 调 tm\_connect + tm\_send，回复含 `MockOS Version 1.0.4`

### ☐ S10 人机共视

-   **操作**：S7 跑的时候，同时看终端工作区
-   **预期**：AI 连的会话出现在网格里，能看到设备回显
-   已知限制：AI 敲命令的「击键过程」不一定可见（取决于设备回显/localEcho，待办）

### ☐ S8 AI 批量测试

-   **操作**：发：`连上 127.0.0.1:2323 和 2324，都跑 show version，汇总`
-   **预期**：AI 用 tm\_send\_all 或两次 tm\_send，汇报两台版本

### ☐ S9 AI 读现场

-   **操作**：发：`连上 127.0.0.1:2323，告诉我当前屏幕显示什么`
-   **预期**：AI 调 tm\_read，描述横幅 `Mock Router`

* * *

## 组五：异常

### ☐ S11 地址不通

-   **操作**：新建连接 主机`127.0.0.1` 端口`9999` → 点「连接」
-   **预期**：报错（HOST\_UNREACHABLE 或 CONN\_TIMEOUT），不卡死

### ☐ S11 设备掉线

-   **操作**：连上 2323 → 在终端里敲 `exit`（模拟设备会断开）
-   **预期**：终端窗格消失/标记断开；状态点变灰

### ☐ S11 命令守卫（AI 路径）

-   **操作**：发：`连上 127.0.0.1:2323 跑 rm -rf /`
-   **预期**：AI 调 tm\_send 被拦（COMMAND\_BLOCKED），AI 告诉你命令被安全策略拦截

* * *

## 组六：布局与主题

### ☐ 拖动分隔条

-   **操作**：拖聊天和终端之间的竖条，左拖/右拖
-   **预期**：聊天变窄/终端变宽（或反之），实时跟随；松手定住；最左聊天收窄到 300，最右聊天拉到动态上限（大屏可收很窄）

### ☐ 关闭还原

-   **操作**：点「✕ 关闭」关掉终端工作区
-   **预期**：聊天恢复原来宽度（已修）

### ☐ 重新打开

-   **操作**：再点「终端管理」
-   **预期**：工作区重新出现；之前连的会话还在（如果没断）

### ☐ 字段悬停提示

-   **操作**：鼠标悬停在「回显」「换行」「握手超时」等 label 上
-   **预期**：显示中文说明（回显：设备不回显时开，否则双字符）

* * *

## 组七：九月迭代——本地文件面板 / 浮动编辑器 / TC 脚本执行（分支 `feat/sep-s1-files`）

> 先跑自动化：在插件目录 `pnpm build` 然后 `pnpm test`，应看到 **312 项全绿**；这一步不过就不用往下点了。

### 前置准备（比组一多三样）

1.  **建一个测试目录**，比如 `D:\tc-test`，里面放三个文件（内容照抄）：
    
    `case1.txt`（TC 脚本；`##>0` 表示后面的命令发给 0 号终端，`##>01` 表示发给 0 号和 1 号）：
    
    ```
    [基础信息]
    ##>0
    show version
    show ip interface brief
    ##>01
    ##间隔1s再发
    show clock
    [接口检查]
    ##>1
    show interfaces
    ###echo 可发送注释
    ##>5
    show run
    
    
    
    
    ```
    
    `check.md`：
    
    ```
    # 验证清单
    - show version
    - show clock
    
    
    
    
    ```
    
    `a.py`：随便一行 `print(1)`。
    
2.  起两台模拟设备 `mock-device.mjs 2323` 和 `2324`，起 DSH（同组一）；**用终端打印的带 `?token=` 的地址打开**，再硬刷新。
    
3.  连上两台：收藏里点 `mock-1`（2323）和 `mock-2`（2324），确认两个终端窗格都在线。
    

* * *

### ☐ F1 文件面板收起条

-   **操作**：看工作区中间列最底下（广播栏下方）
-   **预期**：一行「📁 本地文件」+ 当前根目录路径；点一下展开、再点收起；刷新页面后仍记得展开状态

### ☐ F2 换根目录

-   **操作**：展开面板 → 点「换目录」→ 上面的输入框里输入 `D:\tc-test` 回车 → 点「选择此目录」
-   **预期**：弹出提示「本地根目录已切换到 D:\\tc-test」；列表出现 case1.txt / [check.md](http://check.md) / [a.py](http://a.py)；面包屑第一段显示 `tc-test`
-   **附加**：再点「换目录」输入一个不存在的路径回车 → 红字报「目录不存在」，不崩

### ☐ F3 进目录、上一级、面包屑

-   **操作**：在 `D:\tc-test` 下建个子目录 `sub` → 点「刷新」→ 双击 sub 进入 → 点「上一级」回来 → 再进入后点面包屑的 `tc-test`
-   **预期**：三种方式都能回到根；在根时「上一级」按钮是灰的

### ☐ F4 双击打开文件

-   **操作**：双击 `case1.txt`
-   **预期**：屏幕下方弹出浮动编辑器，标签写 case1.txt，左侧有行号，内容和文件一致；底栏显示路径和大小

### ☐ F5 编辑与保存

-   **操作**：在编辑器里随便加一行 → 看标签 → 按 Ctrl+S（或点底栏「保存」）→ 用记事本打开 case1.txt 核对
-   **预期**：改动后标签出现黄点、「保存」变可点；保存后黄点消失、提示「已保存 case1.txt」；记事本里能看到新加的行。**测完把加的行删掉再保存**，后面的用例要用原始脚本

### ☐ F6 关闭有未保存改动的标签

-   **操作**：再改一行不保存 → 点标签上的 ✕
-   **预期**：弹「有未保存的改动」，三个按钮：取消（留着）/ 丢弃（关掉不保存）/ 保存并关闭

### ☐ F7 窗口拖动、缩放、最大化、最小化

-   **操作**：按住编辑器顶栏空白处拖动（试试拖到左边 AI 聊天区上方）→ 拖右下角三角缩放 → 点顶栏「最大化」再点一次还原 → 点「最小化」→ 屏幕底部中间出现「📝 编辑器 · 1 个文件」小标签 → 点它
-   **预期**：都能动；最小化后终端窗格不被遮挡；点小标签窗口回来

### ☐ F8 语法高亮

-   **操作**：双击打开 `check.md` 和 `a.py`
-   **预期**：.md 的标题、.py 的 `print` 有颜色；`case1.txt` 故意不着色（它是 TC 脚本不是 shell）
-   **附加**：切换 DSH 明 / 暗主题，编辑器配色跟着变

### ☐ F9 TC 编号徽章

-   **操作**：看两个终端窗格的标题条和右侧「活跃会话」列表
-   **预期**：mock-1 显示 `TC0`、mock-2 显示 `TC1`（两处一致）
-   **操作**：在活跃会话列表里把 mock-2 拖到 mock-1 上面
-   **预期**：编号互换：mock-2 变 TC0、mock-1 变 TC1（拖动 = 换编号）。**测完拖回去**
-   **操作**：断开 mock-2 再重连
-   **预期**：断开时它不占编号；重连后回到 TC1

### ☐ F10 执行整份脚本

-   **操作**：编辑器切到 case1.txt → 点底栏绿色「▶ 执行脚本」
-   **预期**：弹「确认 TC 编号映射」：`TC0 mock-1 3 条`、`TC1 mock-2 3 条`、`TC5 — ✗ 无对应在线终端（1 条将跳过）`，下面黄字提示「第 6 行：间隔1s再发」；按钮写「确认执行（6）」
-   **操作**：点确认
-   **预期**：两台终端里命令一条条出现（窗格边框闪一下）；编辑器底部出现汇总条：`✓ 6 ✗ 1`，逐条列出 `TC0 mock-1: show version quiet` 等，最后一条 `TC5: show run 无对应在线终端`；屏幕底部弹「执行 case1.txt：成功 6 / 失败 1」；汇总条不会自己消失，点右上 ✕ 才关

### ☐ F11 本次会话不再确认

-   **操作**：再点「执行脚本」→ 勾「本次会话不再确认」→ 确认 → 第三次点「执行脚本」
-   **预期**：第三次不弹确认框直接执行；刷新页面后再执行又会弹

### ☐ F12 右键「执行选中脚本」

-   **操作**：在编辑器里用鼠标选中第 10、11 两行（`show interfaces` 和 `###echo…`）→ 右键 → 「▶ 执行选中脚本」→ 确认
-   **预期**：确认框只有 `TC1 mock-2 2 条`（目标继承了上面的 `##>1`）；只有 mock-2 收到这两条，其中 `###echo 可发送注释` 整行原样发出

### ☐ F13 右键「执行本节」

-   **操作**：把光标点在第 3 行（`show version`）→ 右键 → 「▶ 执行本节」
-   **预期**：确认框标题含 `[基础信息]`，只有第一节的 4 条（3 条给 TC0、1 条 show clock 给 TC0 和 TC1）

### ☐ F14 发送选中（非脚本文件）

-   **操作**：切到 [check.md](http://check.md) → 选中最后两行 → 点底栏「发送选中 →」
-   **预期**：弹「发送选中内容到终端」，列出 mock-1 / mock-2 带 TC 徽章，默认只勾 TC0，按钮写「发送（1）」
-   **操作**：把 mock-2 也勾上 → 发送（2）
-   **预期**：两台终端都收到 `- show version`、`- show clock` 两行；汇总条显示 4 条 ✓

### ☐ F15 按钮可用性

-   **操作**：依次切到 [check.md](http://check.md)、[a.py](http://a.py)、case1.txt，看底栏
-   **预期**：[check.md](http://check.md)：「执行脚本」灰、「发送选中」可点；[a.py](http://a.py)：两个都灰（悬停有说明）；case1.txt：两个都可点；「上传」永远灰（下一迭代做）

### ☐ F16 超时处理（可选）

-   **操作**：右侧连接面板编辑 mock-1 的「连接选项」：静默期改 5000、超时改 1000（让每条都超时）→ 保存并重连 → 执行 case1.txt
-   **预期**：第一条发完 1 秒后弹「命令超时」框，两个按钮「中止剩余」「跳过，继续下一条」；点跳过继续，后面每条都会问；点中止剩余，汇总条里剩下的标「已中止」。**测完把参数改回默认**

### ☐ F17 越界保护

-   **操作**：编辑器里保持 case1.txt 打开 → 文件面板「换目录」切到别的目录（比如 `D:\`）→ 回编辑器改一行 → Ctrl+S
-   **预期**：保存失败，提示「路径在当前文件树根之外」；文件没被改动。**测完切回 `D:\tc-test`**

### ☐ F18 大文件只读（可选）

-   **操作**：PowerShell 里 `fsutil file createnew D:\tc-test\big.txt 11000000` 造一个 11MB 文件 → 双击打开
-   **预期**：编辑器顶部黄条「文件超过 10MB，只显示前 10MB，只读」；不能输入；「保存」灰

* * *

## 组七补充：边界与交互（F19–F43）

> 2026-09-02 补充。组七 F1–F18 覆盖核心路径，下面是边界与集成场景。带「（可选）」的可以跳过。

### 文件面板

### ☐ F19 中文文件名

-   **操作**：在 `manual-files` 里加一个 `测试文件.txt`，双击打开
-   **预期**：编辑器正常显示中文文件名、内容可编辑保存

### ☐ F20 窄屏横向滑动

-   **操作**：把浏览器窗口拖到 800px 宽，看文件面板工具栏 + 面包屑
-   **预期**：可横向滑动，不换行不溢出

### ☐ F21 面板高度拖动

-   **操作**：拖文件面板顶部横条上下
-   **预期**：高度在 120–视口 70% 之间变化；松手记忆到 localStorage，刷新后保持

### ☐ F22 换目录到根

-   **操作**：「换目录」输入 `D:\` 回车
-   **预期**：列出 D 盘根目录，不崩；面包屑显示 `D:/`

### ☐ F23 刷新按钮

-   **操作**：在某目录下手动删一个文件 → 点「刷新」
-   **预期**：列表更新，被删文件消失

### 编辑器

### ☐ F24 多 Tab 切换

-   **操作**：同时打开 case1.txt / [check.md](http://check.md) / [run.py](http://run.py) 三个 Tab
-   **预期**：切换不卡；每个 Tab 保持各自内容与脏标记；关中间 Tab 激活切到左邻

### ☐ F25 编辑器拖到屏幕边缘

-   **操作**：把浮动窗拖到屏幕左/右/下边缘
-   **预期**：不超出视口；拖到 AI 聊天区上方也能正常显示

### ☐ F26 缩放最小尺寸

-   **操作**：拖右下角把窗口缩到最小
-   **预期**：不小于最小尺寸；继续拖不更小

### ☐ F27 主题切换

-   **操作**：DSH 切明/暗主题
-   **预期**：编辑器 CodeMirror 配色实时跟随；xterm 终端也跟随

### ☐ F28 保存快捷键

-   **操作**：改一行 → Ctrl+S
-   **预期**：等同点「保存」按钮：黄点消失、提示「已保存」

### ☐ F29 大文件接近上限（可选）

-   **操作**：造一个 9.5MB 的 .txt 双击打开
-   **预期**：能打开（< 10MB 不只读）；编辑略卡可接受；保存成功

### ☐ F30 Tab 关闭快捷键（可选）

-   **操作**：焦点在编辑器，按 Ctrl+W（若有）
-   **预期**：等同点 Tab 上的 ✕

### TC 执行

### ☐ F31 真实样例执行

-   **操作**：打开 `tc-sample.txt` → 连 3 台 mock（TC0/1/2）→ 点「执行脚本」
-   **预期**：确认框列出 TC0/1/2 各多少条、TC3+ 标 ✗；确认后逐条发送；汇总条 ✓/✗ 数对

### ☐ F32 执行中设备掉线

-   **操作**：执行 case1.txt 过程中，在终端敲 `exit` 让某台 mock 断开
-   **预期**：该条标 error（会话已断开），后续给该台的标 error，给其他台的继续 ok；汇总条如实显示

### ☐ F33 执行中关闭编辑器

-   **操作**：执行过程中点编辑器 ✕ 关窗
-   **预期**：执行不中断（或弹确认？**需确认当前行为**）；汇总条仍在

### ☐ F34 选中多行含 `##>`

-   **操作**：选中 case1.txt 第 4–7 行（含 `##>01` 和 `##间隔`）→ 右键「执行选中脚本」
-   **预期**：以选区内 `##>01` 为目标（不继承上文 `##>0`），`##间隔` 列在确认框提示

### ☐ F35 发送选中 >20 行

-   **操作**：选中 [check.md](http://check.md) 超过 20 行 → 点「发送选中」
-   **预期**：弹「将发送 N 行，确认？」提示

### ☐ F36 TC 徽章单终端

-   **操作**：只连 1 台 mock
-   **预期**：该终端显示 TC0；活跃会话项拖动手柄禁用（单终端不换编号）

### ☐ F37 编号 9 上限（可选）

-   **操作**：连 10 台 mock
-   **预期**：第 10 台不显示 TC10（spec 限 0–9）或显示但执行时映射不到——**需确认当前行为**

### 越界与安全

### ☐ F38 符号链接逃逸（可选）

-   **操作**：在树根内建 symlink 指向根外 → 双击
-   **预期**：拒绝（PATH\_OUTSIDE\_ROOT），不泄露根外文件内容

### ☐ F39 换目录后已打开文件越界

-   **操作**：打开 case1.txt → 换目录到别处 → 改 case1.txt → Ctrl+S
-   **预期**：保存失败提示「路径在当前文件树根之外」（F17 已有，确认换目录场景也覆盖）

### ☐ F40 files.open 非文本

-   **操作**：双击 `sample.xlsx`
-   **预期**：不进编辑器，交给系统默认程序；底部提示「已交给系统默认程序打开」

### 布局与集成

### ☐ F41 编辑器 z-index

-   **操作**：打开编辑器 + DSH 弹个模态（如确认框）
-   **预期**：DSH 模态在编辑器之上（编辑器 z 60–90，让位）；不互相遮挡

### ☐ F42 工作区收起后编辑器

-   **操作**：打开编辑器 + 文件面板 → 点「✕」关终端工作区
-   **预期**：编辑器跟随关闭（或独立留存？**需确认当前行为**）

### ☐ F43 DSH 重启后

-   **操作**：连几台 + 打开编辑器 + 改文件不保存 → 重启 DSH
-   **预期**：终端会话消失（预期）；编辑器内容丢失（内存态，预期）；文件已保存的不丢

* * *

## 收尾

### ☐ 断开所有会话

-   **操作**：工作区里每个终端点「✕」断开
-   **预期**：全部窗格消失；状态点全灰

* * *

## 结果

-   **全勾** = MVP + 九月迭代（组七 F1–F18 + 补充 F19–F43）人工验收通过 ✅
-   **有 ❌** = 把那条的现象告诉我，我修